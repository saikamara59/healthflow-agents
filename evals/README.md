# evals

Scaffold for agent evaluation harnesses (quality scoring of agent outputs,
prompt-regression evals). Nothing here yet.

HealthFlow's existing translate eval (`healthflow/evals/translate/` in the
host repo) is a candidate for migration once the TranslationAgent port
stabilizes.
